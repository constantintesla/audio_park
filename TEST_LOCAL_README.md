# Локальное тестирование

Скрипт `test_local.py` предназначен для локального тестирования системы анализа речи. Он обрабатывает аудиофайлы локально и отправляет результаты на API сервер.

## Возможности

- ✅ Локальная обработка аудиофайлов (WAV, MP3, OGG)
- ✅ Отправка результатов на API сервер
- ✅ Сохранение сырых данных (опционально)
- ✅ Сохранение результатов в JSON файл
- ✅ Подробный вывод процесса обработки

## Установка

Убедитесь, что все зависимости установлены:

```bash
pip install -r requirements.txt
```

## Использование

### Базовое использование

```bash
# Обработка файла с отправкой на сервер
python test_local.py audio.wav
```

### С указанием URL сервера

```bash
# Указать URL API сервера
python test_local.py audio.wav --api-url http://localhost:5000
```

### С указанием пользователя

```bash
# Указать имя пользователя и ID
python test_local.py audio.wav --username test_user --user-id 123
```

### Только локальная обработка (без отправки на сервер)

```bash
# Обработка без отправки результатов
python test_local.py audio.wav --no-send
```

### Сохранение результата в файл

```bash
# Сохранить результат в JSON файл
python test_local.py audio.wav --output result.json
```

### Без сохранения сырых данных

```bash
# Ускорить обработку, не сохраняя сырые данные
python test_local.py audio.wav --no-raw-data
```

### Тихий режим

```bash
# Минимальный вывод
python test_local.py audio.wav --quiet
```

## Параметры командной строки

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `audio_file` | Путь к аудиофайлу (обязательный) | - |
| `--api-url` | URL API сервера | Из переменной окружения `API_URL` или `http://localhost:5000` |
| `--username` | Имя пользователя | `test_user` |
| `--user-id` | ID пользователя | `0` |
| `--no-send` | Не отправлять на сервер | `False` |
| `--no-raw-data` | Не сохранять сырые данные | `False` |
| `-o, --output` | Путь для сохранения JSON результата | Вывод в stdout |
| `-q, --quiet` | Тихий режим | `False` |

## Переменные окружения

Скрипт использует переменные окружения из файла `.env`:

- `API_URL` - URL API сервера (по умолчанию: `http://localhost:5000`)

## Примеры использования

### Пример 1: Базовый тест

```bash
python test_local.py test_audio.wav
```

Вывод:
```
🔍 Начало обработки файла: test_audio.wav
🌐 API URL: http://localhost:5000

📊 Шаг 1: Локальная обработка аудио...
✅ Локальный анализ завершен
   DSI Score: 2.5
   Риск ПД: Низкий

📤 Шаг 2: Отправка результатов на сервер http://localhost:5000...
✅ Результаты успешно отправлены на сервер
   Всего результатов на сервере: 5
```

### Пример 2: Тест с сохранением результата

```bash
python test_local.py test_audio.wav --output test_result.json --username john_doe --user-id 123
```

### Пример 3: Только локальный анализ

```bash
python test_local.py test_audio.wav --no-send --output local_result.json
```

### Пример 4: Тест удаленного сервера

```bash
python test_local.py test_audio.wav --api-url https://your-server.com:5000
```

## Использование через скрипты

### Windows (TEST_LOCAL.bat)

```cmd
TEST_LOCAL.bat audio.wav
TEST_LOCAL.bat audio.wav --api-url http://localhost:5000
```

### Linux/Mac (TEST_LOCAL.sh)

```bash
./TEST_LOCAL.sh audio.wav
./TEST_LOCAL.sh audio.wav --api-url http://localhost:5000
```

## Формат результата

Скрипт возвращает JSON с следующей структурой:

```json
{
  "timestamp": "2024-01-15T12:00:00",
  "audio_file": "audio.wav",
  "success": true,
  "local_analysis": {
    "features": {...},
    "dsi": {...},
    "symptom_scores": {...},
    ...
  },
  "server_response": {
    "status_code": 200,
    "response": {
      "status": "success",
      "message": "Результат сохранен",
      "total": 5
    }
  },
  "error": null
}
```

## Устранение неполадок

### Ошибка подключения к серверу

Если сервер недоступен, убедитесь что:
1. API сервер запущен (`python start_api.py`)
2. URL сервера указан правильно
3. Нет проблем с firewall/сетью

### Ошибка обработки аудио

Если возникает ошибка при обработке:
1. Проверьте формат аудиофайла (поддерживаются WAV, MP3, OGG)
2. Убедитесь, что файл не поврежден
3. Проверьте, что все зависимости установлены

### Ошибка сохранения сырых данных

Если возникают проблемы с сохранением сырых данных:
1. Проверьте права доступа к директории `results/`
2. Убедитесь, что достаточно места на диске
3. Используйте `--no-raw-data` для пропуска сохранения

## Интеграция с CI/CD

Скрипт можно использовать в CI/CD пайплайнах:

```yaml
# Пример для GitHub Actions
- name: Test audio processing
  run: |
    python test_local.py test_audio.wav --no-send --output test_result.json
    # Проверка результата
    python -c "import json; data=json.load(open('test_result.json')); assert data['success']"
```
