# Анализатор речи для выявления симптомов болезни Паркинсона

Система анализа речи, специализирующаяся на обнаружении симптомов болезни Паркинсона на основе акустических характеристик голоса. Основана на исследованиях:

- **Little et al. 2004**: 104 акустических признака
- **Daoudi 2022**: Monopitch и phonatory instability
- **NIH 2025**: 12 вокальных биомаркеров для оценки прогрессии
- **Российские ИИ-инструменты скрининга (2023-2025)**

## Возможности

- **Telegram бот** для удобного анализа голоса через голосовые сообщения
- **Веб-интерфейс** для просмотра результатов всех пользователей
- **DSI (Dysphonia Severity Index)** - объективный показатель тяжести дисфонии
- Обработка аудиофайлов (WAV/MP3/OGG) с автоматическим ресемплированием до 16kHz
- Извлечение акустических признаков:
  - **Jitter** (вариация периода, >1.1% указывает на ПД)
  - **Shimmer** (вариация амплитуды, >7.5% указывает на ПД)
  - **HNR** (Harmonics-to-Noise Ratio, <13dB указывает на breathy voice)
  - **F0** (основная частота и её вариация)
  - **MPT** (максимальное время фонации)
  - Скорость речи и паузы
  - Спектральные характеристики

- Анализ симптомов (оценка 0-3):
  - Гипофония (низкая громкость)
  - Monopitch (монотонность)
  - Monoloudness (монотонная громкость)
  - Охриплость (hoarseness)
  - Неточная артикуляция

- Оценка риска ПД на основе количества превышенных пороговых значений
- Структурированный JSON отчет на русском языке

## Установка

### Вариант 1: Docker (рекомендуется)

**Локальная разработка:**
```bash
# Создайте .env файл
cp env.example .env
nano .env  # Укажите TELEGRAM_BOT_TOKEN

# Запустите
docker-compose --env-file .env up -d
```

**Production (хостинг):**
```bash
# Используйте production конфигурацию
docker-compose -f docker-compose.prod.yml --env-file .env up -d --build
```

**Подробные инструкции:** [DEPLOY.md](DEPLOY.md)

### Вариант 2: Локальная установка

```bash
pip install -r requirements.txt
```

## Быстрый старт

### 🐳 Запуск в Docker (рекомендуется)

**Самый простой способ:**

```bash
# Linux/Mac
chmod +x START.sh
./START.sh

# Windows
START.bat

# Или вручную
docker-compose up -d
```

**Веб-интерфейс:** http://localhost:5000

**Примечание:** Токен Telegram бота должен быть указан в переменной окружении `TELEGRAM_BOT_TOKEN` (см. `env.example`).

### 💻 Локальный запуск (без Docker)

**Быстрый способ:**

```bash
# Windows
START_LOCAL.bat

# Linux/Mac
chmod +x START_LOCAL.sh
./START_LOCAL.sh
```

**Или вручную:**

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Настройте переменные окружения:**

Создайте файл `.env` на основе `env.example`:
```bash
cp env.example .env
nano .env  # Укажите TELEGRAM_BOT_TOKEN
```

3. **Запустите API сервер** (терминал 1):
```bash
python start_api.py
```

4. **Запустите Telegram бота** (терминал 2):
```bash
python start_bot.py
```

5. **Откройте веб-интерфейс:** `http://localhost:5000`

### Использование из командной строки:

```bash
python parkinson_analyzer.py audio_file.wav
```

Сохранить результат в файл:
```bash
python parkinson_analyzer.py audio_file.wav -o report.json
```

### Локальное тестирование с отправкой на сервер:

Для тестирования обработки аудио с автоматической отправкой результатов на API сервер используйте скрипт `test_local.py`:

```bash
# Базовое использование
python test_local.py audio.wav

# С указанием URL сервера
python test_local.py audio.wav --api-url http://localhost:5000

# Только локальная обработка (без отправки на сервер)
python test_local.py audio.wav --no-send

# С сохранением результата в файл
python test_local.py audio.wav --output result.json
```

**Windows:**
```cmd
TEST_LOCAL.bat audio.wav
```

**Linux/Mac:**
```bash
./TEST_LOCAL.sh audio.wav
```

Подробная документация: [TEST_LOCAL_README.md](TEST_LOCAL_README.md)

### 4. Программное использование:

```python
from parkinson_analyzer import ParkinsonAnalyzer

analyzer = ParkinsonAnalyzer()
result = analyzer.analyze_audio_file("audio.wav")
print(result)
```

## Формат вывода

Система выдает JSON отчет следующего формата:

```json
{
  "audio_summary": {
    "duration_sec": 45.2,
    "sample_rate": 16000,
    "segments": 12
  },
  "features": {
    "jitter_percent": 2.3,
    "shimmer_percent": 8.1,
    "hnr_db": 10.5,
    "rate_syl_sec": 2.8,
    "f0_sd_hz": 42
  },
  "symptom_scores": {
    "hypophonia": 3,
    "monopitch": 2,
    "monoloudness": 2,
    "hoarseness": 1,
    "imprecise_articulation": 2,
    "pd_risk": "Высокий (89%, согласно Little 2004 + Daoudi 2022)"
  },
  "report": [
    "- Гипофония (тяжелая): низкий RMS (0.015), типично для ПД [Little 2004].",
    "- Monopitch (умеренный): низкая вариация F0 (SD=42.0Hz) [Daoudi 2022].",
    "- Рекомендация: консультация невролога, LSVT логопедия..."
  ],
  "visuals": {
    "waveform": "Данные: 720000 точек, длительность 45.00с",
    "spectrogram": "Частоты: 0-8000Hz, временные кадры: 882"
  }
}
```

## DSI (Dysphonia Severity Index)

Система рассчитывает DSI по формуле:
```
DSI = 0.13 × MPT + 0.0053 × F0-High - 0.26 × I-Low - 1.18 × Jitter(%) + 12.4
```

**Интерпретация DSI:**
- **+2…+5**: Нормальный голос
- **0…+2**: Легкая дисфония
- **-2…0**: Умеренная дисфония (PD 1-2)
- **<-2**: Тяжелая дисфония (PD 3-5)

**Параметры DSI:**
| Параметр | Норма | При Паркинсоне |
|----------|-------|----------------|
| MPT (сек) | >15-20 | <10 (слабость дыхания) |
| F0-High (Гц) | >400-500 | <300 (monopitch) |
| I-Low (дБ) | <45 | >55 (тихий голос) |
| Jitter (%) | <1 | >1.5-2 (дрожь) |

DSI коррелирует с Voice Handicap Index и идеален для мониторинга терапии (LSVT LOUD).

## Пороговые значения

Система использует следующие пороговые значения на основе исследований:

| Признак | Порог | Интерпретация |
|---------|-------|---------------|
| Jitter | >1.1% | Нестабильность голосовых связок |
| Shimmer | >7.5% | Вариация амплитуды |
| HNR | <13dB | Breathy voice |
| F0 SD | <50Hz | Monopitch |
| Скорость речи | <3 сл/сек | Медленная речь |
| Паузы | >30% | Проблемы с артикуляцией |

При ≥3 признаках, превышающих пороги: **89% точность** (Daoudi 2022).

## Зависимости

- `librosa`: Обработка аудио и спектральный анализ
- `parselmouth`: Извлечение F0 и анализ pitch через Praat
- `numpy`: Численные вычисления
- `scipy`: Фильтрация сигналов
- `soundfile`: Чтение аудиофайлов
- `aiogram`: Telegram бот framework
- `flask`: Веб-сервер и API
- `flask-cors`: CORS поддержка для API
- `requests`: HTTP запросы

## Структура проекта

```
audio_park/
├── bot.py                 # Telegram бот
├── api.py                 # API сервер и веб-интерфейс
├── parkinson_analyzer.py  # Основной анализатор
├── feature_extractor.py   # Извлечение признаков
├── symptom_analyzer.py    # Анализ симптомов
├── audio_processor.py     # Обработка аудио
├── start_bot.py          # Запуск бота
├── start_api.py          # Запуск API
├── index.html            # Веб-интерфейс
├── requirements.txt      # Зависимости
├── results.json          # Хранилище результатов (создается автоматически)
├── results/              # Директория с сырыми данными каждого анализа
│   ├── {result_id}/      # Папка для каждого анализа
│   │   ├── original.*    # Исходный аудиофайл
│   │   ├── processed_audio.wav  # Обработанный аудио
│   │   ├── segments/     # Сегменты аудио
│   │   ├── waveform_data.json   # Данные waveform
│   │   ├── spectrogram_meta.json # Метаданные спектрограммы
│   │   └── segment_features.json # Сырые признаки по сегментам
└── results/              # Директория для дополнительных файлов
```

## Хранение результатов

Все результаты анализа сохраняются в файле **`results.json`** в корне проекта. 

Каждая запись содержит:
- Информацию о пользователе (Telegram username, user ID, дата/время)
- Все акустические признаки
- DSI (Dysphonia Severity Index) с параметрами
- Оценку симптомов (0-3)
- Оценку риска ПД
- Полный текстовый отчет
- **Сырые данные** (если включено сохранение):
  - Исходный аудиофайл
  - Обработанный аудио (после редукции шума)
  - Сегменты аудио
  - Сырые признаки по сегментам (до усреднения)
  - Данные waveform и метаданные спектрограммы

### Сохранение сырых данных

По умолчанию система сохраняет все сырые данные каждого анализа в директории `results/`. Каждый анализ получает уникальную папку с ID вида `{user_id}_{timestamp}`.

**Структура сырых данных:**
```
results/
├── {result_id}/
│   ├── original.*          # Исходный аудиофайл (формат зависит от источника)
│   ├── processed_audio.wav # Обработанный аудио (16kHz, моно, после редукции шума)
│   ├── segments/           # Сегменты аудио
│   │   ├── segment_000.wav
│   │   ├── segment_001.wav
│   │   └── ...
│   ├── waveform_data.json   # Данные waveform (амплитуда, время)
│   ├── spectrogram_meta.json # Метаданные спектрограммы (размеры, диапазоны)
│   └── segment_features.json # Сырые признаки каждого сегмента (до усреднения)
```

**Отключение сохранения сырых данных:**
```python
analyzer = ParkinsonAnalyzer(save_raw_data=False)
```

**Доступ к сырым данным:**
В каждом результате в `results.json` есть поле `raw_data` с информацией о сохраненных файлах:
```json
{
  "raw_data": {
    "result_id": "123456_20240101_120000_123",
    "data_directory": "results/123456_20240101_120000_123",
    "files": {
      "original_audio": "results/.../original.ogg",
      "processed_audio": "results/.../processed_audio.wav",
      "segments": ["results/.../segments/segment_000.wav", ...],
      "waveform_data": "results/.../waveform_data.json",
      "spectrogram_meta": "results/.../spectrogram_meta.json",
      "segment_features": "results/.../segment_features.json"
    }
  }
}
```

### Экспорт результатов

Доступны три формата экспорта:

1. **CSV** (`/api/export/csv`) - для анализа в Excel/Google Sheets
2. **JSON** (`/api/export/json`) - полный формат со всеми данными
3. **HTML** (`/api/export/html`) - красивый отчет для просмотра

Кнопки экспорта доступны на веб-интерфейсе.

## API Endpoints

- `GET /api/results` - Получить все результаты
- `POST /api/results` - Сохранить результат анализа
- `GET /api/results/<index>` - Получить конкретный результат
- `GET /api/stats` - Получить статистику
- `GET /api/export/csv` - Экспорт результатов в CSV
- `GET /api/export/json` - Экспорт результатов в JSON
- `GET /api/export/html` - Экспорт результатов в HTML отчет

## Безопасность

⚠️ **ВАЖНО:** Никогда не храните токены в коде!

- Используйте переменные окружения через файл `.env`
- Файл `.env` автоматически исключен из Git
- Если токен был скомпрометирован, немедленно отзовите его через [@BotFather](https://t.me/BotFather) (`/revoke`)

**Подробнее:** [DEPLOY.md](DEPLOY.md#безопасность)

## Устранение неполадок

### Бот не отвечает
- Проверьте, что токен правильный
- Убедитесь, что бот запущен
- Проверьте логи на наличие ошибок

### API не работает
- Проверьте, что порт 5000 свободен
- Убедитесь, что Flask установлен
- Проверьте логи на наличие ошибок

### Ошибки при обработке аудио
- Убедитесь, что все зависимости установлены
- Проверьте, что файл не поврежден
- Убедитесь, что длительность не превышает 60 секунд

## Примечания

- Система предназначена для исследовательских целей и не заменяет медицинскую диагностику
- Требуется качественная аудиозапись (минимум 16kHz)
- Результаты зависят от качества записи и условий записи
- Для работы Telegram бота требуется стабильное интернет-соединение
- API сервер должен быть доступен для бота (настройте порты и firewall при необходимости)

## Лицензия

Исследовательское использование.