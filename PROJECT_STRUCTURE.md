# Структура проекта

```
audio_park/
├── 📄 Основные модули
│   ├── api.py                 # API сервер и веб-интерфейс
│   ├── bot.py                 # Telegram бот
│   ├── parkinson_analyzer.py  # Основной анализатор
│   ├── feature_extractor.py   # Извлечение акустических признаков
│   ├── symptom_analyzer.py    # Анализ симптомов ПД
│   └── audio_processor.py     # Обработка аудио
│
├── 🚀 Скрипты запуска
│   ├── start_api.py           # Запуск API сервера
│   ├── start_bot.py            # Запуск Telegram бота
│   ├── START.sh                # Скрипт запуска Docker (Linux/Mac)
│   └── START.bat               # Скрипт запуска Docker (Windows)
│
├── 🐳 Docker
│   ├── Dockerfile              # Образ для контейнеризации
│   ├── docker-compose.yml     # Конфигурация Docker Compose
│   └── .dockerignore          # Исключения для Docker образа
│
├── 🌐 Веб-интерфейс
│   └── index.html             # Веб-интерфейс для просмотра результатов
│
├── 📚 Документация
│   ├── README.md              # Основная документация
│   ├── SETUP.md               # Инструкции по настройке
│   ├── DOCKER.md              # Инструкции по Docker
│   ├── CHANGELOG.md           # История изменений
│   └── PROJECT_STRUCTURE.md   # Этот файл
│
├── ⚙️ Конфигурация
│   ├── requirements.txt       # Python зависимости
│   └── .gitignore            # Игнорируемые файлы Git
│
└── 💾 Данные (создаются автоматически)
    ├── results.json          # Хранилище результатов анализа
    └── results/              # Директория для дополнительных файлов
```

## Описание файлов

### Основные модули

- **api.py** - Flask API сервер с endpoints для сохранения/получения результатов и экспорта
- **bot.py** - Telegram бот с обработкой голосовых сообщений и отправкой отчетов
- **parkinson_analyzer.py** - Главный класс для анализа речи на симптомы ПД
- **feature_extractor.py** - Извлечение акустических признаков (Jitter, Shimmer, HNR, F0, DSI параметры)
- **symptom_analyzer.py** - Анализ симптомов и оценка риска ПД
- **audio_processor.py** - Загрузка, обработка и сегментация аудио

### Скрипты запуска

- **start_api.py** - Запуск API сервера локально
- **start_bot.py** - Запуск Telegram бота локально
- **START.sh / START.bat** - Скрипты для быстрого запуска Docker контейнеров

### Docker

- **Dockerfile** - Определение Docker образа
- **docker-compose.yml** - Конфигурация для запуска API и бота в контейнерах
- **.dockerignore** - Файлы, исключаемые из Docker образа

### Веб-интерфейс

- **index.html** - Одностраничное веб-приложение для просмотра результатов

### Данные

- **results.json** - JSON файл с результатами всех анализов (создается автоматически)
- **results/** - Директория для временных файлов (создается автоматически)

## Потоки данных

1. **Telegram бот → API** - Бот отправляет результаты анализа через POST /api/results
2. **Веб-интерфейс → API** - Веб-интерфейс получает результаты через GET /api/results
3. **API → results.json** - Все результаты сохраняются в JSON файл
4. **Экспорт** - API предоставляет endpoints для экспорта в CSV, JSON, HTML
